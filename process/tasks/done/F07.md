# F07 — Scheduler Git Sync: Tasks

## T01 — Install pytest-asyncio as dev dependency
**Status**: done
**Description**: `pytest-asyncio` was declared in the `test` optional extra in pyproject.toml but not installed in the dev virtual environment. Run `uv add --dev pytest-asyncio` to make it available. All 7 async scheduler tests were failing with "async def functions are not natively supported" because the plugin was absent.

## T02 — Verify all scheduler tests pass
**Status**: done
**Description**: After installing pytest-asyncio, run `uv run pytest tests/test_scheduler.py -v` and confirm all 44 tests pass. Tests cover SchedulerConfig defaults/env-var overrides, Scheduler init, error logging, retry logic, git credential checking, content copy, change detection, commit/push, async sync_to_github, async sync_raindrops, sync wrapper, and global scheduler singleton.

## T03 — Update F07 status in features.md
**Status**: done
**Description**: Mark F07 as `done | Tests written: yes | Tests passing: yes` in `.j2/features/features.md`. Move it from the incomplete section to the completed section.
