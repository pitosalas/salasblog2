# F26 — Traffic Classification by Visitor Type

## Tasks

### T01 — Define visitor type classifier in visitor_type.py
**Status**: done
Create `src/salasblog2/visitor_type.py` with a `classify_visitor(user_agent: str) -> str` returning one of five types: `"human"`, `"ai_bot"`, `"search_engine"`, `"crawler"`, `"unknown"`. AI bots checked first, then search engines, then browser signatures for human, then generic bot patterns for crawler, empty/unrecognized UA returns unknown.

### T02 — Extend VisitCounter to store counts by visitor type
**Status**: done
Change the storage format in `stats.py` from `{path: count}` to `{path: {visitor_type: count}}`. Update `increment(path, visitor_type)`, `get(path)`, `get_all()` accordingly. Ensure backward compatibility when loading old-format JSON (treat bare integers as `human` counts).

### T03 — Pass User-Agent to increment() in server routes
**Status**: not started
In `server.py`, extract `request.headers.get("user-agent", "")`, call `classify_visitor()`, and pass the result to `get_counter().increment(path, visitor_type)` in all three routes: `serve_home`, `serve_pages_files`, `serve_raindrops_files`.

### T04 — Update admin stats display to show per-type breakdown
**Status**: not started
Update the `/api/stats` JSON response and the `loadStats()` JS in `admin.html` to show per-type columns (Human, Search Engine, AI Bot) in the stats table alongside totals.

### T05 — Write tests in test_visitor_type.py
**Status**: not started
Add tests covering: known search engine UAs return `"search_engine"`, known AI bot UAs return `"ai_bot"`, unknown UA returns `"human"`, empty string returns `"human"`.
