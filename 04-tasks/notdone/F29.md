# Tasks for Feature F29

## T01 — Extract load_json_cache / save_json_cache into utils.py
**Status**: not done
**Description**: In `src/salasblog2/utils.py`, add `load_json_cache(cache_file: Path, env_var: str = None) -> dict` and `save_json_cache(cache_file: Path, cache: dict) -> None`. Extract from `raindrop.py` lines 45–71 (the `_load_cache` / `_save_cache` logic). The `env_var` parameter allows loading an initial timestamp from an environment variable as a fallback. Update `raindrop.py` to call the new functions. Functions must have no side effects beyond the cache file.

## T02 — Extract paginate_api_request into utils.py
**Status**: not done
**Description**: In `utils.py`, add `paginate_api_request(fetch_func, max_items=None, page_size=50) -> list`. Extract from `raindrop.py` `fetch_raindrops()` (lines 101–151). `fetch_func(page, page_size)` is called repeatedly until it returns fewer items than `page_size` or `max_items` is reached. Update `raindrop.py` to use the new helper.

## T03 — Extract reset_directory_and_cache into utils.py
**Status**: not done
**Description**: In `utils.py`, add `reset_directory_and_cache(directory: Path, cache_file: Path) -> None`. Extract from `raindrop.py` `reset_data()` (lines 153–163). Deletes and recreates `directory`, deletes `cache_file` if it exists. Update `raindrop.py` to call it.

## T04 — Write unit tests for all three utilities
**Status**: not done
**Description**: Add `tests/test_utils_cache.py` covering: `load_json_cache` with no file and no env var returns `{}`, with env var returns dict with that value, with existing file returns parsed contents; `save_json_cache` writes valid JSON; `paginate_api_request` stops when fetch_func returns fewer than page_size items, respects max_items; `reset_directory_and_cache` removes and recreates directory and removes cache file.
