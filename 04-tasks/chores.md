# Chores

- [x] Fix `metaweblog_getCategories` hardcoded `["General", "Technology"]` stub — now returns `BLOG_TAGS`, so MarsEdit's category picker reflects this blog's real tag vocabulary instead of two unrelated placeholder values.
- [x] Add a `link` (permalink) field to `blogger_getPost`/`blogger_getRecentPosts` and their MetaWeblog wrappers — MarsEdit's Link field was always empty since the response never included one.
- [x] Fix MetaWeblog `getPost`/`getRecentPosts` hardcoding `categories: ["General"]` for every post — now reports each post's real tags.
- [x] Fix `create_excerpt_with_info()` truncating mid-token (`**bold**`, `[link](url)`, `<tag>`), leaving a dangling marker that printed as literal `**`/`[`/`<` on listing/home pages instead of rendering as formatting.
- [x] Fix `create_excerpt_with_info()` collapsing newlines before truncating — block-level markdown (`## header`, `> quote`, `* bullet`) needs to be at the start of a line to be recognized, so once collapsed it leaked its marker characters as literal text. Now strips those markers per-line first.
- [x] Add the "Read more" link to the home page for truncated excerpts — `blog_list.html` already had this, `home.html` didn't.
- [x] Narrow a bare `except:` to `except Exception:` in `utils.py`'s `_parse_malformed_frontmatter()` (pre-existing style guide violation, fixed while already editing the file).
- [x] Make `BloggerAPI` (`blogger_api.py`) resolve its content directory the same volume-first way `get_content_directory()` (`server.py`) and `SiteGenerator` (`generator.py`) already do. `__init__` now prefers `/data/content/blog` when the volume exists, matching the rest of the codebase; `_backup_to_volume()`/`_delete_from_volume()` deleted entirely since writes/deletes go straight to the volume-resolved `blog_dir` — nothing left to reconcile after the fact. Touched `__init__`, `blogger_newPost`, `blogger_editPost`, `blogger_deletePost`; `metaweblog_newMediaObject`'s separate three-copy media backup logic was left untouched (different concern — media intentionally lives in source/output/volume copies for different reasons). Tests: replaced `TestBackupToVolume` with `TestVolumeFirstContentDir` (verifies `__init__`'s volume-first resolution) and `TestNewEditDeletePost` (verifies writes/deletes land directly, no separate backup/delete-from-volume step).
- [x] Full-repo style-guide pass (`.claude/style_guide.md`): fixed relative imports (`from .utils import ...` → `from salasblog2.utils import ...`) in `generator.py`, `raindrop.py`, `cli.py`, and lazy in-function relative imports in `scheduler.py` (moved to top-level, absolute — no circular-import risk, confirmed via dependency grep); added the missing required file header (shebang/module line/Author/license) to `generator.py`, `raindrop.py`, `cli.py`, `scheduler.py`, `utils.py`, `__init__.py`; ran `ruff check --fix`/`ruff format` across the whole repo (unused imports, extraneous f-string prefixes, unused variables — mostly mechanical, a few required judgment, see below); fixed 2 bare `except:` in `tests/integration/test_server.py`. No test coverage regression — full suite still passes (only the pre-existing unrelated `test_raindrop.py` failure remains).
- [x] Real bug found and fixed during the style-guide pass (not just linting): `scripts/debug_content_dirs.py`'s `main()` had a redundant `from pathlib import Path` inside a `try` block, which made Python treat `Path` as function-local for the *entire* function scope — so the earlier `cwd = Path.cwd()` call (before that local import ever ran) would raise `UnboundLocalError` at runtime. Removed the redundant inner import. No existing test coverage for this standalone debug/troubleshooting script (not part of the package, never imported by the app) — not adding new test infrastructure for it given its scope, but flagging here since it's a real fix, not just a lint fix.
- [x] `tests/deployment/test_checksum_sync.py::test_identical_content_skipped` computed `initial_dest_mtime`/`final_dest_mtime` but never compared them, despite the comment claiming to "verify destination wasn't unnecessarily updated" — a genuine test-coverage gap, not just an unused variable. First attempt at fixing it (asserting the two mtimes are equal) was itself wrong and failed: `rsync -a --checksum` still syncs the destination's mtime to match the source's mtime whenever `-a`'s `-t` timestamp-preservation runs, regardless of whether `--checksum` decided data needed retransferring — so mtime alone can't prove a transfer was skipped. Reverted to just dropping the unused variables, with a comment explaining why an mtime-equality assertion would be wrong.
- [x] Regenerated all `01-literate/*.md` docs from scratch (`utils`, `stats`, `visitor_type`, `propose`, `draft_generator`, `raindrop`, `generator`, `blogger_api`, `scheduler`, `server`, plus `cli` as an appendix) per `.claude/literate.md`'s prompt, in dependency order. Old `06-blogger_api.md` deleted, replaced by `08-blogger_api.md` at its correct dependency-order position.
