# Tasks for Feature F36

## T01 — Add base64 parsing to the XML-RPC dispatcher
**Status**: done
**Description**: In `server.py` `xmlrpc_endpoint()`, add handling for `<base64>` typed values both at the top-level param level and inside struct members. Image data arrives as base64-encoded binary. Decode using `base64.b64decode()`.
**Tests**: Add test in `tests/test_xmlrpc_parsing.py` (new file): send a simulated XML-RPC request body containing a `<base64>` param and verify it is decoded to bytes correctly. Also test a struct containing a `base64` member.

## T02 — Implement metaweblog_newMediaObject in BloggerAPI
**Status**: done
**Description**: Add `metaweblog_newMediaObject(blogid, username, password, struct)` to `blogger_api.py`. `struct` contains `name` (filename), `type` (MIME type), `bits` (bytes). Date-prefix the filename (`2026-04-15-original.jpg`). Save to: (1) `static/images/uploads/` — source, included in git. (2) `output/static/images/uploads/` — served immediately. (3) `/data/static/images/uploads/` — volume backup, raise fault on failure. Return `{'url': '/static/images/uploads/<filename>'}`.
**Tests**: Add tests in `tests/test_blogger_api.py`: (1) file is written to all three locations, (2) returned URL is correct, (3) filename is date-prefixed, (4) volume backup failure raises XML-RPC Fault.

## T03 — Register metaWeblog.newMediaObject in server.py dispatcher
**Status**: done
**Description**: In `server.py` `xmlrpc_endpoint()`, add `elif method_name == "metaWeblog.newMediaObject"` routing to `api.metaweblog_newMediaObject(*params)`.
**Tests**: Add test in `tests/test_xmlrpc_parsing.py`: send a full XML-RPC `metaWeblog.newMediaObject` request to the FastAPI test client and verify a `{'url': ...}` response comes back.

## T04 — Include static/images/uploads/ in git sync
**Status**: done
**Description**: In `scheduler.py` `sync_to_github()`, add `git add static/images/uploads/` alongside the existing `git add content/` so uploaded images are committed and pushed to GitHub.
**Tests**: Add test in `tests/test_scheduler.py`: mock `subprocess.run` and verify that both `git add content/` and `git add static/images/uploads/` are called during `sync_to_github()`.
