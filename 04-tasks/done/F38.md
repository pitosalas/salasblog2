# Tasks for Feature F38

## T01 — Load only the changed content type in incremental_regenerate_post()
**Status**: done
**Description**: In `generator.py:incremental_regenerate_post()`, replaced unconditional loading of all three content types with targeted loading. For a blog edit: loads blog_posts fully, loads raindrops (needed for home page), skips pages entirely. For a raindrop edit: loads raindrops fully, loads blog posts, skips pages. For a page edit: loads pages fully, skips raindrops.

## T02 — Run regeneration asynchronously so save returns immediately
**Status**: done
**Description**: Split `save_content_item()` into file-write only. Extracted `_regenerate_in_background()` as a separate function. All four save/create endpoints (edit-post, new-post, edit-page, new-page) now use FastAPI `BackgroundTasks` to run regeneration after the HTTP response is sent.
